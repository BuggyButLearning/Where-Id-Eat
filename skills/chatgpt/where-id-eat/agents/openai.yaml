name: Where I’d Eat
description: Run a predictable location-centered food guide command using strict research JSON, deterministic rendering, live availability checks, real images, Yelp resolution, maps, and coffee or dessert companions.
version: 2.0.0
